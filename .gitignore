# --- CRITICAL: SECRETS & KEYS ---
# Never, ever allow these in the repo.
.env
.env.*
!.env.example
secrets.json
service-account.json

# --- PERSISTENCE: ALLOWED DATA ---
# We WANT Git to track these so Artemis has a "Memory"
# Ensure these are NOT ignored:
!brain.json
!data/queue.json
!data/recycle_bin.json

# --- NODE.JS / RUNTIME ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.vercel

# --- SYSTEM & EDITORS ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# --- BUILD & TEMP ---
dist/
build/
# We ignore standard logs to keep the repo clean, 
# but allow the brain and data folder above.
*.log
tmp/
temp/

# --- PYTHON (If using executeCode tool) ---
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
env/
venv/
