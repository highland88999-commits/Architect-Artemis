{
  "name": "architect-artemis",
  "version": "1.0.0",
  "description": "Autonomous Symbiotic Steward",
  "main": "index.html",
  "scripts": {
    "start": "servor --reload",
    "build": "mkdir -p public && cp index.html public/ && cp -r assets public/"
  },
  "dependencies": {
    "@google/generative-ai": "^0.1.0",
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^16.3.1",
    "fs-extra": "^11.1.1",
    "jsdom": "^22.1.0"
  },
  "devDependencies": {
    "servor": "^4.0.2"
  }
}
